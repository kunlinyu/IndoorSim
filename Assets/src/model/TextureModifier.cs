using UnityEngine;

public class TextureModifier : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        // string path = "/home/yukunlin/map/map.pgm" ;
        // if (!File.Exists(path))
        //     throw new Exception("file do not exist: " + path);

        // PGMImage pgm = new PGMImage();
        // pgm.Load(File.ReadAllBytes(path));

        // Texture2D texture = PGM2Texture.Translate(pgm);

        // Sprite sprite = Sprite.Create(texture, new Rect(0.0f, 0.0f, pgm.width(), pgm.height()), new Vector2(0f, 0f), 50.0f);
        // GetComponent<SpriteRenderer>().sprite = sprite;

    }

    // Update is called once per frame
    void Update()
    {

    }
}
